@model List<StockWebFinal.Models.Ingreso>

@{
    ViewData["Title"] = "Listado de ingresos";
}

<p>
    <a asp-action="Crear" class="btn btn-primary">Nuevo Ingreso</a>
</p>


<div class="container-lg col-4">
    <h1>Listado de ingresos</h1>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered" id="" width="100%" cellspacing="0">
            <thead class="table-dark">
                <tr>
                    <th>Fecha</th>
                    <th>Producto</th>
                    <th>Cantidad</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody class="table-secondary">
                @foreach (var ingreso in Model)
                {
                    <tr>
                        <td>@ingreso.Fecha</td>
                        <td>@ingreso.Producto_Id</td>
                        <td>@ingreso.Cantidad</td>
                        <td>
                            <a asp-action="Detalles" asp-route-id="@ingreso.Id" class="btn btn-info">Detalles</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No hay registros</p>
    }
</div>