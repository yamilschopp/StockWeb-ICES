@model List<StockWebFinal.Models.Marca>

@{
    ViewData["Title"] = "Listado de Marcas";
}

<p>
    <a asp-action="Crear" class="btn btn-primary">Crear Marca</a>
</p>


<div class="container-lg col-4">
    <h1>Listado de marcas</h1>
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered" id="" width="100%" cellspacing="0">
            <thead class="table-dark">
                <tr>
                    <th>Nombre</th>
                    <th>Fecha Alta</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody class="table-secondary">
                @foreach (var marca in Model)
                {
                    <tr>
                        <td>@marca.Nombre</td>
                        <td>@marca.FechaAlta</td>
                        <td>@marca.Estado</td>
                        <td>
                            <a asp-action="Detalles" asp-route-id="@marca.Id" class="btn btn-info">Detalles</a>
                            <a asp-action="Editar" asp-route-id="@marca.Id" class="btn btn-warning">Editar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No hay registros</p>
    }
</div>